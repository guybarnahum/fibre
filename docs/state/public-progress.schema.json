{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "fibre-public-progress-v2",
  "title": "Fibre public progress",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "canonical",
    "asOf",
    "title",
    "headline",
    "summary",
    "statusDefinitions",
    "population",
    "capabilities",
    "currentWork",
    "truthRules"
  ],
  "properties": {
    "$schema": { "type": "string", "minLength": 1 },
    "schemaVersion": { "const": "fibre-public-progress-v2" },
    "canonical": { "const": true },
    "asOf": { "type": "string", "format": "date" },
    "title": { "type": "string", "minLength": 1 },
    "headline": { "$ref": "#/$defs/twoLevelText" },
    "summary": { "$ref": "#/$defs/twoLevelText" },
    "statusDefinitions": {
      "type": "array",
      "minItems": 5,
      "items": { "$ref": "#/$defs/statusDefinition" }
    },
    "population": { "$ref": "#/$defs/population" },
    "capabilities": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/capability" }
    },
    "currentWork": { "$ref": "#/$defs/currentWork" },
    "truthRules": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "$defs": {
    "status": {
      "enum": ["achieved", "demonstrated", "in_progress", "not_yet", "preserved_failure"]
    },
    "twoLevelText": {
      "type": "object",
      "additionalProperties": false,
      "required": ["simple", "detail"],
      "properties": {
        "simple": { "type": "string", "minLength": 1 },
        "detail": { "type": "string", "minLength": 1 }
      }
    },
    "twoLevelClaim": {
      "type": "object",
      "additionalProperties": false,
      "required": ["claim", "limitation"],
      "properties": {
        "claim": { "type": "string", "minLength": 1 },
        "limitation": { "type": "string", "minLength": 1 }
      }
    },
    "statusDefinition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "simpleLabel", "detailLabel", "meaning"],
      "properties": {
        "id": { "$ref": "#/$defs/status" },
        "simpleLabel": { "type": "string", "minLength": 1 },
        "detailLabel": { "type": "string", "minLength": 1 },
        "meaning": { "$ref": "#/$defs/twoLevelText" }
      }
    },
    "populationItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "count", "status", "simple", "detail"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
        "label": { "type": "string", "minLength": 1 },
        "count": { "type": "integer", "minimum": 0 },
        "status": { "$ref": "#/$defs/status" },
        "simple": { "type": "string", "minLength": 1 },
        "detail": { "type": "string", "minLength": 1 }
      }
    },
    "population": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "summary", "items", "importantNote"],
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "summary": { "$ref": "#/$defs/twoLevelText" },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/populationItem" }
        },
        "importantNote": { "$ref": "#/$defs/twoLevelText" }
      }
    },
    "capability": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "group", "label", "status", "simple", "detail", "evidence"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
        "group": { "enum": ["foundations", "genesis", "development", "society"] },
        "label": { "type": "string", "minLength": 1 },
        "status": { "$ref": "#/$defs/status" },
        "simple": { "$ref": "#/$defs/twoLevelClaim" },
        "detail": { "$ref": "#/$defs/twoLevelClaim" },
        "evidence": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "workLayer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["summary", "currentStep", "nextSteps"],
      "properties": {
        "summary": { "type": "string", "minLength": 1 },
        "currentStep": { "type": "string", "minLength": 1 },
        "nextSteps": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "currentWork": {
      "type": "object",
      "additionalProperties": false,
      "required": ["milestone", "simple", "detail", "finalLifeCognitionAuthorized"],
      "properties": {
        "milestone": { "type": "string", "minLength": 1 },
        "simple": { "$ref": "#/$defs/workLayer" },
        "detail": { "$ref": "#/$defs/workLayer" },
        "finalLifeCognitionAuthorized": { "type": "boolean" }
      }
    }
  }
}
