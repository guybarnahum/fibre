---
id: fibre-current-priorities
status: accepted
last-reviewed: 2026-08-22
canonical: true
---

# Current priorities

For the plain-English public view of what Fibre has and has not achieved, use [`public-progress.md`](public-progress.md) and the canonical machine-readable [`public-progress.json`](public-progress.json). This file is the engineering execution view.

1. **Gate-G(2) remains HOLD, but the five blocking B1-B5 closure findings are now locally closed and the second hostile review is ready.** The first review at `8c8d1c1d7208b4f31a3bea359d103563ce0d29e2` accepted the replacement science/G5-G6 reconciliation and returned a pre-life closure HOLD. The corrected execution candidate was locally verified at `6f4dc0f4ff306e03ff43cc89593efc424274b85c`: both replacement zero-call verifiers CLEAR, `703/703` tests, repository/world validation green, and only preserved local recovery evidence untracked. Second request: `docs/validation/m2-pr39-gate-g2-review-request-round2.md`.

2. **Do not change the scientific/execution candidate while the second Gate-G(2) review is pending.** No replacement World, genome, synthetic parent genome, World↔genome mapping, G2 result, G3 treatment schedule, D3 threshold, provider/model, seed namespace or execution path may change. Documentation-only recording after the verified candidate does not authorize life generation.

3. **Fresh replacement G2 remains CLEAR, frozen and non-rerunnable.** Pair scores are `22/24, 24/24, 24/24, 22/24, 23/24`; all `5/5` measured cycle edges are detectable, every genome is covered, descriptive aggregate `115/120 (95.8%)`. This is pre-life instrument/calibration evidence, not personhood evidence. The replacement source genomes use a more consistently aligned semantic dimension by locus ordinal, so these scores are not directly comparable to the old cohort.

4. **The World↔genome mapping is deterministic but not a derangement.** Realized mapping is `1<-3 2<-2 3<-1 4<-4 5<-5`, with fixed points `[2,4,5]`. Do not rerandomize. Controls are G1-before-G2 ordering, no-geography locus discipline and content-independent digest-ranked within-origin assignment with no human semantic pairing choice.

5. **B1 is closed by a complete replacement execution packet.** The packet contains the fresh G3 treatment instance, replacement G4 cognition/roster binding, one-shot execution binding, hard-pinned output paths/seeds/provider/model and dedicated replacement final-cohort runner. Preflight reports `CLEAR_PACKET_GATE_G2_HOLD`, output root absent and zero provider calls.

6. **B2 is closed: replacement Pass A explicitly selects G4-v3.** Effective policy is one initial version, up to two independent form repairs, up to two independent referential retries, hard total five generated versions. The historical H runner retains its legacy shared-three behavior for reproducibility; replacement execution must explicitly pass `GENESIS_PASS_A_RELIABILITY_POLICY_V3` and reject legacy fallback.

7. **B3 is closed: replacement D3 uses all five measured G2-detectable edges with no pair-3-4 escape hatch.** Both fixed primary ordinals must be at least `4/5`, and at least one must be `5/5`. Any measured edge that is null, errored, tied or unanalyzable is **not correct**. The old pair-3-4 mandatory-disclosure/nonblocking exit has no replacement CLEAR standing.

8. **B4/B5 are disclosure corrections only.** The aligned genome-authoring design/non-comparability is explicit; frozen genomes are unchanged. The assignment is accurately described with fixed points `[2,4,5]`; mapping is unchanged and may not be rerandomized.

9. **The optional Pass-B `uncertainty[*]` genome-copy hardening is NOT part of this replacement experiment.** It was a nonblocking review suggestion. A brief shared-module implementation changed the exact frozen G4-v2 Pass-B source/blob and retry-prompt hash; local tests caught this and the change was retracted. Historical G4-v2 admission authority is preserved exactly. Current gate scans `rememberedContent` only; the `uncertainty[*]` residual leakage surface is explicitly disclosed in `rg4-residual-gate-g2-disclosures-v1.json` and `rg4-pass-b-genome-copy-closure-amendment-v1.json` (`not_applied_preserved_review_note`).

10. **Replacement execution uses Birth Center journal replay only at its proven scope.** It supports process-restart replay of committed invocation identities. Do **not** claim host-crash/power-loss fsync durability. Terminal result/failure ends the one-shot attempt; restart may continue only the same in-progress attempt without resetting scientific retry budgets.

11. **Five replacement Threads remain unborn in the #39 sense.** They have frozen Worlds, origin classes, Thread/Genesis identities, symbolic genomes, assignment, G3 schedule and a fully bound pre-life execution packet, but still have zero replacement childhood episodes, zero memories, zero meanings and zero published lives.

12. **The only next scientific action is the second blocking Gate-G(2) review.** No replacement provider call, Pass-A/B/C cognition or `publishBirth()` is authorized while HOLD. Send the reviewer `docs/validation/m2-pr39-gate-g2-review-request-round2.md` and require `CLEAR`, `HOLD` or `REDESIGN` against the actual repository.

13. **A second Gate-G(2) CLEAR would authorize exactly one replacement final-life attempt—nothing more.** Weak, boring, stereotyped or poorly differentiated Threads remain evidence and cannot trigger quality regeneration. After the first mechanically valid five-Thread cohort freezes, run frozen diagnostics and close #39 under the predeclared rules.

14. **Keep old H-v2 and recovery evidence immutable and separate.** H-v2 remains the original final-cohort operational HOLD; recovery remains the terminal resilience-only HOLD. Preserve the local untracked `artifacts/validation/m2-pr39/h/recovery-v1/`; do not delete it or commit it blindly.

15. **#40 may proceed architecturally in parallel; #41 remains blocked.** #40 owns canonical bounded consumption of rich identity/history into cognition. #41 owns Whole-Person standing. #39 deliberately does not move the Whole-Person score; current checkpoint remains 15/26.

16. **Keep public progress synchronized at verdict boundaries.** Public language remains: the five unborn Threads passed their inherited-difference test; the first pre-birth review found setup gaps and stopped life generation. The closure is now locally green and awaiting a second review, but do not present the HOLD as cleared until the reviewer returns CLEAR.