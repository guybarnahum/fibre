---
id: fibre-current-priorities
status: accepted
last-reviewed: 2026-08-22
canonical: true
---

# Current priorities

For the plain-English public view of what Fibre has and has not achieved, use [`public-progress.md`](public-progress.md) and the canonical machine-readable [`public-progress.json`](public-progress.json). This file is the engineering execution view.

1. **Gate-G(2) remains HOLD after the second hostile review.** Round 2 at `e6ab8c0b8636c1d8d17741e6cba7233b067d6304` again found no REDESIGN trigger. B2-B5 are settled; the sole blocker is C1: the replacement executable preflight declared inherited G3/G4/G5/G6 authority but did not bind every consumed inherited file at execution time. Canonical result: `docs/validation/m2-pr39-gate-g2-review-result-round2.md`.

2. **Close C1 without changing scientific input.** This is an enforcement-only correction: call the existing freeze verifiers from the authorized replacement entrypoint, cross-check the actual G4 runtime/history fields against the replacement binding, verify actual G3 production/analysis digests, bind the inherited Pass-B input helper source, and widen post-CLEAR drift detection to inherited `g/protocol` plus `tools/genesis`.

3. **The reviewed generation implementation is preserved byte-for-byte behind the new authority gate.** The round-2 replacement runner implementation is now `tools/genesis/genesis-replacement-final-cohort-core.mjs` at exact Git blob `81d89fb17eca549106bd51ea0aba2d8329bacb80`. The authorized path remains `tools/genesis/genesis-replacement-final-cohort.mjs`; it verifies inherited authority before delegating to the preserved core. The execution binding pins both path and blob.

4. **Do not touch consumed scientific evidence.** The five replacement Worlds, source/child genomes, World↔genome mapping, fresh G2 result, G3 treatment positions, accepted D3 threshold structure, provider/model and cognition prompts remain frozen. No final-life outcome exists to adapt to.

5. **Fresh replacement G2 remains CLEAR, frozen and non-rerunnable.** Scores are `22/24, 24/24, 24/24, 22/24, 23/24`; all five measured cycle edges are detectable and every genome is covered. This is pre-life genome/instrument evidence only. Scores are not directly comparable to the old cohort because the replacement loci use a more consistently aligned per-ordinal authoring design.

6. **B2 remains closed: replacement Pass A explicitly selects G4-v3.** Effective policy is one initial version, up to two independent form repairs, up to two independent referential retries, hard total five generated versions. The historical H runner retains its legacy behavior only for reproducibility.

7. **B3 remains closed: replacement D3 has five blocking edges and no pair-3-4 escape hatch.** Both primary ordinals must be at least `4/5`; at least one must be `5/5`. Null/error/tie/unanalyzable measured edges are `not_correct`. The executable preflight now requires the exact four replacement carve-out-retirement statements by content, not merely list length.

8. **B4/B5 remain disclosure-only closures.** The aligned genome-authoring design/non-comparability is explicit; genomes are unchanged. The deterministic mapping is accurately described as `1<-3 2<-2 3<-1 4<-4 5<-5` with fixed points `[2,4,5]`; no rerandomization is allowed.

9. **The optional Pass-B `uncertainty[*]` generation-time hardening remains NOT APPLIED.** Historical G4-v2 Pass-B authority remains exact. Instead, the replacement packet now requires a read-only post-generation, pre-diagnostic four-token scan of every genome-exposed `uncertainty[*]` against the assigned frozen genome. A confirmed leak invalidates the affected inference and cannot trigger regeneration.

10. **Publication semantics are now stated precisely.** All five generation bundles must complete before publication begins. `publishBirth()` is atomic per Thread, but publication is **not cohort-atomic** across all five Threads. A publication-phase failure can therefore leave already-published Threads plus terminal HOLD; it does not authorize a replay/replacement cohort.

11. **Post-CLEAR drift protection must cover everything the attempt consumes.** The authorized entrypoint now watches `services/world-kernel/src`, `services/birth-center/src`, all `tools/genesis`, inherited `artifacts/validation/m2-pr39/g/protocol`, and replacement protocol. A future witness `reviewedHead` must resolve to a real Git commit and strictly predate/be an ancestor of the executing HEAD.

12. **Replacement execution still claims process-restart replay only.** Birth Center journal replay can reuse committed invocation identities without resetting budgets. Do not claim host-crash/power-loss fsync durability. Terminal result/failure ends the one-shot attempt.

13. **Five replacement Threads remain unborn in the #39 sense.** They have frozen Worlds, identities, genomes, assignment and pre-life execution material, but zero replacement childhood episodes, memories, meanings and published lives. Final-life cognition remains unauthorized.

14. **Next step is local zero-call verification of the C1 correction.** Run the replacement closure verifier, replacement preflight, full tests and repo validation. Preserve `artifacts/validation/m2-pr39/h/recovery-v1/` untracked. Do not create a third Gate-G(2) request until that exact correction head is locally green.

15. **After local green, request a short third blocking Gate-G(2) review.** The remaining question should be mechanical: does the authorized executable path now verify every inherited input it consumes? Only a third-review CLEAR may authorize exactly one replacement final-life attempt.

16. **#40 may proceed architecturally in parallel; #41 remains blocked.** #40 owns canonical bounded consumption of rich identity/history into cognition. #41 owns Whole-Person standing. #39 deliberately does not move the Whole-Person score; current checkpoint remains 15/26.

17. **Keep public progress synchronized.** Simple English should now say: the five unborn Threads passed their inherited-difference test; two pre-birth reviews have stopped us before childhood generation; the second found one remaining execution-check gap; we are fixing that check before asking again. Do not present Gate-G(2) as cleared until an external reviewer returns CLEAR.
