---
id: m2-pr39-replacement-v2-r1-rereview-result
status: accepted
reviewed-candidate: ef9a1bf399280dc3f33a73490f91d3e63c3198d0
reviewed-head: 6fd360b7290e1256b2e63248c872a67ffafda190
verdict: CLEAR
last-reviewed: 2026-08-23
---

# PR #39 replacement-v2 R1 corrected hostile re-review result

## Verdict

`VERDICT: CLEAR`

The corrected R1 substrate is cleared for **R2 implementation only**. This verdict authorizes no provider/model call, replacement-v2 generation, diagnostics, publication, or reuse of replacement-v1 generated life.

The reviewer independently verified that the exact implementation candidate was `ef9a1bf399280dc3f33a73490f91d3e63c3198d0` and that `ef9a1bf..6fd360b` contained only the R1 re-review request and engineering-state documentation.

Local/reviewer verification at the candidate:

- replacement-v2 R1 preflight: `CLEAR_R1_HOLD_CORRECTION_PRE_REVIEW_ZERO_CALL`;
- tests: `700 passed / 0 failed`;
- repository and World seed validation passed;
- five envelope digests matched the preregistered corrected values and were stable across six rebuilds;
- zero provider calls;
- replacement-v2 cognition remained unauthorized.

## Blocking findings closed

The re-review found B1-B8 closed without outcome-driven bound changes:

1. **Protocol/code binding:** every historical-envelope policy field is bound to the enforcing implementation, in both tightening and loosening directions.
2. **Chronology:** all fourteen windows are bound to inherited `bornAt`, exact internal seams, declared ages, generated span and chronology end.
3. **EventStructure authority:** inherited v2 and effective v3 digests are pinned, including source-v2 drift.
4. **Timezone authority:** each slot is bound to its exact IANA timezone; same-offset substitutes are refused.
5. **Diagnostic horizon translation:** treated ordinals remain 3 and 6 and translate to replacement-v2 horizons 8 and 14 without changing the five-edge D3 decision rule.
6. **Continuity derivation:** participant/place evidence, introduction chronology, role authority, duplicate refusal and chronological aggregation are fail-closed and computed rather than asserted.
7. **Young-adult counterpart semantics:** v3 counterpart mode is derived from each structure's reviewed `accessModes`; no coverage bound was changed in response to review.
8. **Provider schema projection:** `uniqueItems`, `minLength`, `maxLength` and `maxItems` are removed from the OpenAI transport schema while canonical constraints remain locally enforced.

The corrected external-counterpart opportunity counts are 7, 9, 8, 7 and 9. The reviewer judged the change correctly causal: it follows from completing the young-adult `self_directed` mapping, not from tuning a coverage threshold.

## Development flattening

The reviewer found no current invariant lost by removing undeployed H2/replacement-v1 execution and recovery runtimes. The deleted paths had no remaining live references, frozen evidence remained intact, and removal made consumed replacement-v1 execution authority less reachable rather than more reachable.

The accepted development rule remains: frozen artifacts may retain versioned evidence identity; parallel executable compatibility implementations require a real persisted-data, API/wire or migration boundary.

## R1 nonblocking finding promoted to R2 gate

**N1 — D3 threshold integers must be load-bearing before diagnostic authority exists.** The reconciliation artifact states the unchanged rule as both treated ordinals at least 4/5 measured core edges correct and at least one treated ordinal 5/5, but R1 did not mechanically bind the two integers. R2 must assert:

- `eachOrdinalMinimumCorrectCoreEdges === 4`;
- `atLeastOneOrdinalCorrectCoreEdges === 5`;
- and should refuse a contradictory threshold statement.

This is a hard R2 diagnostic-authority condition, not permission to change the threshold.

## R2 obligations authorized by this CLEAR

R2 may now implement, with zero provider calls:

- the current replacement-v2 runner around the reviewed historical envelope/realization substrate;
- the exact sparse-history semantics on every replacement-v2 Pass-B cognition call;
- atomic durable participant/place continuity at Thread birth;
- provider-schema projection plus local canonical enforcement in execution authority;
- replacement-v2 diagnostic binding to the 8/14 reconciliation and unchanged D3 threshold;
- a complete zero-call R2 execution preflight and hostile review packet.

A separate R2 CLEAR is required before any model/provider call.

## Scientific standing

This R1 CLEAR establishes substrate integrity only. It does not establish that #39 has succeeded scientifically, confer Whole-Person standing, or authorize publication of a replacement cohort.
