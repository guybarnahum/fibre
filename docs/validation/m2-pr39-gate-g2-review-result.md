---
id: m2-pr39-gate-g2-review-result
status: superseded_by_round2_hold
last-reviewed: 2026-08-22
canonical: false
---

# Milestone #39 — first blocking Gate-G(2) review result

## Verdict

```text
HOLD
```

This document preserves the **first** Gate-G(2) hostile review. It is historical evidence, not the current gate state.

Current Gate-G(2) authority is the second review result:

```text
docs/validation/m2-pr39-gate-g2-review-result-round2.md
```

The first review found no REDESIGN trigger and accepted the replacement science/G5-G6 five-edge reconciliation, but stopped final-life generation for five correctable pre-life closure findings B1-B5.

Reviewed head:

```text
8c8d1c1d7208b4f31a3bea359d103563ce0d29e2
```

Reviewer-reported verification:

```text
699 tests · 699 passed · 0 failed
Repository validation passed.
World seed validation passed.
replacement reconciliation verifier CLEAR — ZERO CALL
```

No replacement provider call, childhood episode, memory, meaning or birth was authorized.

## First-review findings

### B1 — complete replacement execution packet missing

The reviewed packet had fresh G1/G2 material and G5/G6 reconciliation but no concrete replacement G3 treatment instance, G4/execution binding, output root or executable replacement cohort runner.

### B2 — G4-v3 reliability not bound to the actual cohort call site

The historical H runner omitted `generationPolicy`, so it used the deliberate legacy shared-three-version default. Replacement execution needed explicit `GENESIS_PASS_A_RELIABILITY_POLICY_V3` selection with independent `2 form / 2 record / 5 total` bounds.

### B3 — old pair-3-4 G6 carve-out still reachable

Replacement-effective CLEAR semantics needed to retire the old pair-3-4 nonblocking path and define null/error/tie/unanalyzable measured edges as `not_correct`.

### B4 — replacement genome-authoring template not disclosed

The replacement genomes used a more consistently aligned per-ordinal semantic dimension design. Fresh G2 remained valid for the replacement cohort itself, but its scores could not be treated as directly comparable to the old cohort.

### B5 — mapping inaccurately called a derangement

The realized deterministic content-independent mapping was:

```text
1 <- 3
2 <- 2  fixed point
3 <- 1
4 <- 4  fixed point
5 <- 5  fixed point
```

The false derangement description had to be retired without rerandomization.

## Scientific judgment retained

The first reviewer explicitly accepted the main G5/G6 reconciliation:

- G5 was already a five-edge instrument;
- the old pair-3-4 exemption reflected only the old cohort's weak G2 ceiling on that edge;
- the replacement freeze prohibited importing old empirical pair detectability before fresh G2 existed;
- with all five fresh edges detectable, `>=4/5` at both primary ordinals plus `5/5` at least once is the band-structural image of the old `>=3/4` plus `4/4` rule;
- the illustrative independence reference becomes stricter (`0.0107421875` vs `0.03515625`).

That scientific judgment was independently retained by the second review.

## Closure outcome

B1-B5 were addressed without rewriting the replacement Worlds, genomes, mapping, fresh G2 result, treatment schedule or accepted D3 threshold structure. The corrected execution candidate was locally verified at:

```text
6f4dc0f4ff306e03ff43cc89593efc424274b85c
```

with:

```text
replacement closure verifier: CLEAR B1-B5 — ZERO CALL
replacement preflight: CLEAR_PACKET_GATE_G2_HOLD
703 tests · 703 passed · 0 failed
repository/world validation passed
final-life cognition: NOT AUTHORIZED
```

The second review then found one new outer-layer enforcement gap, C1, while accepting B2-B5 as genuinely closed. See `m2-pr39-gate-g2-review-result-round2.md` for current authority.

## Historical nonblocking note

The first review suggested extending the four-token Pass-B verbatim genome-copy gate from `rememberedContent` to `uncertainty[*]`. That optional hardening was briefly implemented, but local tests showed it changed exact frozen G4-v2 historical authority. It was fully retracted before round 2. Historical G4-v2 admission remains byte-exact; `uncertainty[*]` is handled as an explicit residual disclosure and post-generation scan obligation rather than a changed generation-time gate.
