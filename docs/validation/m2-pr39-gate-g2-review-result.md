---
id: m2-pr39-gate-g2-review-result
status: hold_pre_life_closure
last-reviewed: 2026-08-22
canonical: false
---

# Milestone #39 — first blocking Gate-G(2) review result

## Verdict

```text
HOLD
```

The replacement scientific design survived the hostile review. The G5/G6 five-edge reconciliation was judged legitimate and non-adaptive. The HOLD is a **pre-life closure HOLD**: the reviewed packet was incomplete and contained several execution/disclosure ambiguities that must be removed before any replacement childhood or life cognition.

No replacement provider call, childhood episode, memory, meaning or birth is authorized by this result.

Reviewed head:

```text
8c8d1c1d7208b4f31a3bea359d103563ce0d29e2
```

Reviewer-reported verification at that head:

```text
699 tests · 699 passed · 0 failed
Repository validation passed.
World seed validation passed.
replacement reconciliation verifier CLEAR — ZERO CALL
```

The hostile review made zero provider calls and reported that it did not use semantic content from the three completed H-v2 lives to justify any replacement-protocol change.

## Blocking findings

### B1 — complete replacement execution packet missing

The reviewed `replacement-v1` packet had fresh G1/G2 material and the G5/G6 reconciliation, but no concrete replacement G3 treatment instance, G4/execution binding, output root or executable replacement cohort runner.

**Required posture:** freeze the complete execution surface before re-review so Gate-G(2) can inspect exactly what a CLEAR would authorize.

### B2 — G4-v3 reliability not bound to the cohort call site

The historical H runner calls `generateRichPassAEpisode()` without an explicit `generationPolicy`, so the runtime deliberately falls back to the legacy shared three-generated-version budget. The G4-v3 policy is opt-in by design and therefore was only documentary authority, not effective replacement execution policy.

**Required posture:** the replacement Pass-A call site must explicitly pass `GENESIS_PASS_A_RELIABILITY_POLICY_V3`, and preflight must mechanically prove the effective policy is `pr39-g4-pass-a-reliability-amendment-v3` with independent `2 form / 2 record / 5 total` bounds.

### B3 — old pair-3-4 G6 carve-out still reachable

The first reconciliation correctly made all five fresh G2-detectable edges part of the D3 core, but the inherited frozen G6 `clearRule` still contained four-edge language and allowed the old pair-3-4 null/error limitation as mandatory disclosure. A future pair-3-4 tie/error could therefore have supported two readings after the outcome existed.

**Required posture:** replacement effective CLEAR semantics must explicitly retire the pair-3-4 carve-out, restate the five-edge rule, and define null/error/tie/unanalyzable measured core edges as **not correct**.

### B4 — replacement genome authoring template was not disclosed

The replacement loci use a more consistently aligned semantic dimension by ordinal across source genomes than the original cohort. The reviewer found this plausibly explains part of the stronger fresh G2 ceiling.

This does **not** invalidate the replacement G2 ceiling for the replacement cohort itself. It does mean the fresh numerical ceiling is not directly comparable to the original cohort's scores.

**Required posture:** disclose the six aligned authoring dimensions and condition eventual D3 interpretation on that design. Do not rewrite frozen genomes.

### B5 — assignment was called a derangement but realized three fixed points

The replacement mapping is deterministic and content-independent but is not a mathematical derangement:

```text
1 <- 3
2 <- 2  fixed point
3 <- 1
4 <- 4  fixed point
5 <- 5  fixed point
```

No human pairing discretion was exercised; the fixed points are the deterministic outcome of the frozen digest-ranking rule.

**Required posture:** correct the description, disclose fixed points `[2,4,5]`, and rely on the actual controls: G1-before-G2 ordering, non-geographic loci and content-independent digest assignment. Do not rerandomize after observing the mapping.

## Reconciliation judgment retained

The reviewer explicitly accepted the main G5/G6 seam:

- G5 was already a five-edge instrument before the replacement;
- the old pair-3-4 exemption existed only because the old cohort's G2 ceiling was weak there;
- the replacement freeze prohibited importing old empirical pair detectability **before** the fresh G2 ceiling existed;
- with all five fresh edges detectable, `>=4/5` at both primary ordinals plus `5/5` at least once is the band-structural image of the old `>=3/4` plus `4/4` rule;
- the illustrative independence reference becomes stricter rather than easier (`0.0107421875` vs `0.03515625`).

That scientific interpretation remains the replacement authority unless a later pre-life Gate-G(2) review rejects the corrected packet.

## Nonblocking findings taken into the correction

The maintainer elected to harden these review findings before re-review because they change integrity/closure rather than life-quality selection:

- extend the existing four-token Pass-B genome-copy boundary to `uncertainty[*]` as well as `rememberedContent` before Pass C;
- bind the replacement runner to the Birth Center durable adapter for **process-restart replay of committed invocations** while explicitly declining host-crash/fsync durability claims;
- hardcode and bind the replacement execution-binding path rather than expose the historical unpinned environment override;
- disclose that the five-edge structural translation is stricter under the illustrative chance arithmetic;
- name `pass_a_record_repair_changed_facts` as a terminal operational HOLD for the one replacement attempt;
- ensure failure-artifact write failure cannot replace/mask the original generation error.

Other nonblocking observations are retained for disclosure/review rather than silently changing scientific model inputs. In particular, the replacement correction does not opportunistically alter the existing Pass-B arm-label exposure.

## Correction artifacts

The pre-life closure work is intentionally additive around already-consumed evidence. The frozen G2 protocol/result, Worlds, genomes and assignment are not rewritten.

```text
artifacts/validation/m2-pr39/replacement-v1/protocol/rg2-authoring-assignment-disclosure-amendment-v1.json
artifacts/validation/m2-pr39/replacement-v1/protocol/rg3-pass-b-treatment-instance-v1.json
artifacts/validation/m2-pr39/replacement-v1/protocol/rg4-cognition-execution-binding-v1.json
artifacts/validation/m2-pr39/replacement-v1/protocol/rg4-pass-b-genome-copy-closure-amendment-v1.json
artifacts/validation/m2-pr39/replacement-v1/protocol/rg5-g6-closure-amendment-v2.json
artifacts/validation/m2-pr39/replacement-v1/protocol/replacement-execution-binding-v1.json
tools/genesis/genesis-replacement-final-cohort.mjs
tools/genesis/genesis-replacement-gate-g2-closure-verify.mjs
```

The old G5/G6 freezes and first replacement reconciliation remain preserved as historical authority/evidence; replacement-effective interpretation is made explicit by the closure amendment rather than pretending the old text never existed.

## Authorization state

Until a **new blocking Gate-G(2) review returns CLEAR**:

```text
replacement Pass-A cognition:    forbidden
replacement Pass-B cognition:    forbidden
replacement Pass-C cognition:    forbidden
replacement publishBirth():      forbidden
replacement G2 rerun:            forbidden
replacement genome rewrite:      forbidden
replacement assignment rerandomization: forbidden
final-life cognition authorized: false
```

The five replacement Threads remain unborn in the #39 sense.

## Next gate

After local tests, repository validation, the replacement final-cohort zero-call preflight and the B1–B5 closure verifier are all green, issue a **second Gate-G(2) review request** against the complete corrected packet. A re-review CLEAR may authorize exactly one replacement final-life attempt and does not itself grant Whole-Person or causal standing.
