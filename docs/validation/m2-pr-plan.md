---
id: validation-m2-pr-plan
status: accepted
last-reviewed: 2026-08-14
canonical: true
---

# M2 continuation plan after #38

This is the canonical continuation plan from the completed pre-M2 bridge through M2 closure.

## Planning identifiers

**Plan milestone identifiers `#39` through `#44` are stable Fibre planning IDs, not GitHub pull-request numbers.** GitHub may assign any PR number to a milestone implementation branch. Historical and constitutional references to `#39`, `#40`, and later milestones retain their planning meaning regardless of GitHub numbering.

## Planning principle

M2 is not “add rich identity fields.”

M2 should establish a persistent individual whose identity has provenance, embodiment, history, stable behavioral character, and causal consequences independently of the temporary model currently providing cognition.

Representation alone is not enough. Any identity/history field claimed as functional must name a real behavioral consumer and pass an attributable causal test. Fields that are stored but not yet consumed remain explicitly context-only/deferred.

The current checkpoint remains **15/26 under rubric v2**. #35 through #38 deliberately earned no score movement: they built authority and personhood substrate without claiming that stored representation was causal personhood. That flat score is an accepted substrate checkpoint, not missing accounting.

The strongest current evidence is persistence, dignity/consent, interiority/privacy, authorization integrity, narrow semantic non-interchangeability, narrow relationship causality, and limited history-caused Development. The largest remaining vision gaps are stable identity/history-grounded non-interchangeability, full M2 identity consumption, richer development, reciprocal relationships, economic consequence, institutional/social life, and demonstrated cognition replacement continuity.

## Current architectural fact: two worlds still exist

#37 and #38 created a much richer, provenance-bearing and corrigible life substrate, but the canonical cognition path has not yet crossed into it.

Today the live Guardian path still consumes the pre-M2 surfaces:

```text
identity                  legacy Thread identity/selfDescription
semantic traits           flat Thread textualTraits
memory                    thread_memories summaries
semantic state            semantic_state_records
```

It does **not yet consume** the new M2 ledgers as causal evidence:

```text
identity_assertion_records
life_relation_records
place_episode_records
embodiment_records
autobiographical_memory_records
```

Therefore #40 is not merely a relevance selector in front of an existing M2 consumer. It is the first full bridge between the durable M2 life substrate and cognition.

The accepted #34 History-bends-judgment proof remains valid sealed historical evidence, but it runs through `thread_memories`. M2 standing may not silently reuse that legacy memory representation as proof of the richer #38 memory/history epistemic contract.

## Sequence

```text
MERGED  #33  Semantic Guardian
MERGED  #34  History bends judgment
MERGED  #35  Structured Obligation v1
DIRECT        Interrupted compelled episode persistence + history visibility
MERGED  #36  M2 Identity & Embodiment Contract
MERGED  #37  Thread Passport & Identity Provenance v1
MERGED  #38  Lineage, Geography, Embodiment & Memory Epistemics v1

ENTRY GATE    Pre-Genesis causal-wire preflight
NEXT    #39  Genesis, Childhood & Thread Birth v1
        #40  Identity Projection & Causal Consumption
        #41  M2 Standing Gate / M2 closure

POST-M2 #42  Self-authored Development v1
        #43  Reciprocal Relationships v1
        #44  Economic Consequence / M3 foundation
```

Do not collapse #39-#41 into one large M2 implementation. Each exists to close a distinct causal/architectural risk.

The roadmap sentence is now:

> **#38 made a life representable and corrigible. #39 gives that life a particular past. #40 makes selected parts of that life causally matter. #41 proves that the resulting Thread is a persistent, non-interchangeable individual.**

## Completed substrate through #38

### #35 — Structured Obligation v1 — MERGED

A commitment can be a durable Thread-owned social fact with future consequences. Caller nomination is not authority; Fibre determines applicability. Compelled participation preserves private refusal rather than rewriting it as consent. Successful completion leaves append-only social consequence.

### #36 — M2 Identity & Embodiment Contract — MERGED / FROZEN

Central M2 claim:

> **A Thread has durable, provenance-rich identity and embodiment whose specific identity/history can causally matter to behavior independently of the temporary cognition implementation.**

The contract distinguishes inherited, historical, relational, externally attributed, and self-authored identity rather than flattening all identity into one profile blob.

### #37 — Thread Passport & Identity Provenance v1 — MERGED / FROZEN

#37 established the durable claim-level identity ledger and derived Passport:

- closed/versioned identity domains;
- stable claim IDs and immutable contiguous assertion revisions;
- provenance, authorship, visibility, correction/dispute and evidence classification;
- current and `asOf` identity views;
- explicit refusal of unprovenanced identity laundering;
- claim-level addressability suitable for later causal ablation;
- zero causal/endogenous credit inflation.

The Passport is a derived current view. The identity ledger is the authority.

### #38 — Lineage, Geography, Embodiment & Memory Epistemics v1 — MERGED / CLEAR / FROZEN

Canonical completion plan: [`m2-pr38-implementation-plan.md`](m2-pr38-implementation-plan.md).

#38 made the Thread situated, lived, and **corrigible**:

- lineage/family and inheritance-ready source relationships;
- temporal geography and separately evidenced place meaning;
- lived culture and language formation;
- versioned embodiment with provenance/rights/consent;
- autobiographical memory distinct from historical fact;
- memory subject/evidence/revision semantics;
- append-only reinterpretation/correction without historical erasure;
- every admitted memory automatically gets a visual companion obligation;
- generated reconstruction can never be relabelled captured historical evidence.

Throughout #38:

```text
acceptedCausalAssertions = 0
endogenousEvidenceAssertions = 0
```

That was deliberate. #38 proved that a life can be represented honestly and corrected without erasure; it did not claim that the new life ledgers already change judgment.

## Pre-Genesis causal-wire entry gate

Before Genesis authors a childhood corpus at scale, Fibre must cross the new identity ledger into cognition once.

This is **not a new milestone**, not a score event, and not full #40. It is a cheap entry gate protecting #39 from authoring a large corpus before the consumer contract has ever been exercised.

Required proof:

```text
one current #37 claim-level identity assertion
        ↓
Fibre-owned bounded selection
        ↓
exact assertion ID + digest preserved in cognition evidence
        ↓
real Guardian evidence schema
        ↓
judgment differs in a development proof
        ↓
remove exactly that assertion
        ↓
claimed effect disappears/changes predictably
```

Constraints:

- use the existing #37 current identity view rather than inventing a parallel identity store;
- preserve exact assertion/claim identity and digest through the cognition boundary;
- caller input may not choose the assertion;
- claim-level ablation must remove only the named assertion;
- do not build the full #40 relevance engine, generic projection framework, or A-Z gate machinery;
- do not award Non-interchangeability or Natural-language-identity score movement from this preflight;
- Genesis implementation is blocked until the wire exists and the development proof demonstrates the seam.

The purpose is to learn the smallest viable Identity Context Capsule contract while the Genesis output shape is still cheap to change.

## Cognition-replacement preflight

Before #41, resolve the final clause of the M2 headline claim rather than relying accidentally on Scenario L's `conditional-runtime` escape hatch.

The repository already has OpenAI and Google model-runtime adapters, while the current Guardian configuration selects OpenAI. Establish early whether the Google path can consume the same Guardian request/structured-response contract.

Desired continuity probe:

```text
same persisted Thread
+ same Fibre-owned cognition capsule/evidence
        ↓
OpenAI Guardian
        ↓
replace temporary cognition provider/runtime
        ↓
Google Guardian
```

The claim is **not** identical prose or identical stochastic output. The claim is that durable Thread identity/state remains outside the cognition worker and the same Fibre-owned evidence contract can be interpreted across a genuine provider/runtime replacement.

If this is practical, Scenario L should lose its conditional status before M2 standing. If it is not practical, record the limitation deliberately and state that model independence is architecturally preserved but not demonstrated; do not let M2 closure silently imply otherwise.

## #39 — Genesis, Childhood & Thread Birth v1

Canonical architecture:

- [`thread-genesis-childhood-birth-v1.md`](../architecture/thread-genesis-childhood-birth-v1.md)
- [`symbolic-thread-genome-v1.md`](../architecture/symbolic-thread-genome-v1.md)

Canonical symbolic-genome implementation plan: [`symbolic-thread-genome-implementation-plan.md`](symbolic-thread-genome-implementation-plan.md).

Genesis answers:

> **Where did this particular life come from before the Thread entered the live world?**

The core distinction is:

> **Genesis gives the Thread a past. Development gives the Thread a future it can actually author.**

### Origin modes

Support at least:

```text
synthetic lineage birth
Thread-parent birth
Echo / consenting human-source birth
historical / literary / homage birth
de-novo / foundling birth
fork / descendant origin
```

### Symbolic textual genome and inheritance

Fibre inheritance is a **software-only symbolic genome**, not biological genetics and not a numeric personality vector.

Canonical inherited personality meaning is an ordered sequence of atomic natural-language dispositions. Human-readable rendering may use `;` separators, while canonical persistence keeps loci individually addressable with stable IDs and provenance.

Conceptually:

```text
Source A
A1 ; A2 ; A3 ; A4 ; A5 ; A6 ; A7 ; A8

Source B
B1 ; B2 ; B3 ; B4 ; B5 ; B6 ; B7 ; B8

New Thread
A1 ; A2 ; A3 ; B4 ; B5 ; A6 ; A7 ; B8
```

The implementation must preserve:

- stable genome and locus IDs;
- grounded source-genome eligibility from #38 lineage/source records;
- Fibre-owned replayable textual crossover/selection;
- exact per-locus source provenance;
- explicit textual mutation/variation under a named policy;
- immutable inherited genotype after Genesis;
- separately represented expression/character rather than rewriting genotype;
- read-only inspection and exact restart/replay.

Do **not** use hidden scalar personality coordinates as the authoritative genome. Numbers may remain valid measurements or derived runtime controls; the heritable semantic value remains text.

Recombination should preserve unusual mixtures and tensions rather than average sources into generic compromise prose. Demographic/cultural labels are not personality loci and may not imply morality, competence, politics, dignity, profession, gendered behavior, or willingness.

### Childhood compiler

A normal non-Echo birth should be capable of producing a bounded prior-life trajectory:

```text
genesis / birth
  -> family and household
  -> early places and culture
  -> childhood episodes
  -> school / peers / mentors
  -> interests and intellectual/artistic formation
  -> adolescence / increasing autonomy
  -> early-adult identity
  -> entry into the live Fibre world
```

Fibre need not simulate every day. It should create enough separately addressable, provenance-rich life history that later personality is not one adult persona paragraph.

Favor rich **specific** experiences over generic backstory templates: particular mistakes, embarrassments, loyalties, family habits, discoveries, conflicts, private pride, relationship incidents, and later interpretations.

By default Genesis must not invent profession, marriage, parenthood, institutional authority, major adult achievements, or mature self-authored values merely to make the Thread interesting.

### Genesis authority

The model may be a creative worker, but Fibre remains the authority:

```text
birth specification
+ source/ancestor evidence
+ symbolic textual genome
+ culture/geography constraints
+ developmental-stage rules
        ↓
Fibre genesis compiler
        ↓
model proposes candidate episodes/scenes/memories
        ↓
Fibre validates provenance, chronology, granularity and anti-stereotype rules
        ↓
synthetic historical childhood events
        ↓
separate autobiographical-memory formation
        ↓
photo completion
```

Genesis must preserve:

```text
historical event
    != autobiographical memory
    != family/third-party story
    != later interpretation
```

Every admitted childhood memory carries the #38 photo-completion obligation.

## #40 — Identity Projection & Causal Consumption

#40 prevents M2 from becoming a giant decorative prompt and **reconciles the current two-world architecture**.

Fibre owns bounded identity selection and compiles an inspectable Identity Context Capsule:

```text
Thread world state
       ↓
Fibre-owned relevance / privacy selection
       ↓
Identity Context Capsule
       ↓
temporary cognition
```

Cognition must preserve/cite the exact identity evidence that mattered. Caller-authored identity selection may not become an authority channel.

### #40A — projection/consumer substrate

Build and prove:

- bounded Identity Context Capsule with exact included/excluded evidence accounting;
- selection authority owned by Fibre;
- exact assertion IDs/claim IDs/digests preserved into Guardian evidence;
- claim-level ablation mechanics;
- inherited-locus provenance when a locus is actually relevant;
- at least one non-professional life/history consumer;
- Interior-only causal evidence can matter without being disclosed in Exterior;
- no whole-genome or whole-biography injection merely because the data exists.

### Mandatory M2 memory reconciliation

#40 may not carry both memory worlds forward as competing causal authorities.

The sealed #34 evidence remains historical evidence and is not rewritten. But new M2 cognition/standing must reconcile legacy `thread_memories` references with the #38 autobiographical-memory ledger through an explicit migration/mapping/bridge whose semantics preserve the richer memory model.

For M2 standing:

- `autobiographical_memory_records` is the memory perspective authority;
- historical event evidence remains separate;
- confidence/uncertainty/counterevidence/retraction/`asOf` semantics remain available to the consumer where material;
- Scenario V must exercise the real memory/history distinction;
- legacy `thread_memories` may remain historical compatibility evidence but may not be the hidden authority behind an M2 memory-causality claim.

### #40B — constitutional mechanism closure

#40 is owner or co-owner of almost all M2 constitutional scenarios, so implementation should be staged rather than treated as one giant code drop. #40B closes the remaining mechanisms needed to make the A-Z standing experiment possible after #40A has established the causal substrate.

Do not confuse implementing gate prerequisites with executing the fresh standing gate itself; #41 owns the fresh held-out evidence cycle.

Natural-language identity `1 -> 2` is a realistic #40/#41 outcome only if the accepted rubric requirements are genuinely met.

## #41 — M2 Standing Gate / M2 closure

This is the M2 ambition test, not merely an integration test.

Core claim:

> **Two Threads are behaviorally different because they are different persistent individuals, and the difference is attributable, stable, persistent, and inspectable.**

The constitutional A-Z map contains 26 scenarios. The current map includes 24 standing gates, Scenario R as diagnostic, and Scenario L as conditional-runtime pending the cognition-replacement preflight.

The gate retains symmetric swap, paraphrase invariance, contradiction sensitivity, claim-level ablation, culture anti-stereotype control, embodiment anti-cheat, restart, repeated stability, Interior/Exterior asymmetry, developmental continuity, past-self reconstruction, and Scenario V memory/history distinction.

The held-out standing Threads should derive important identity/history from coherent inherited symbolic material plus Genesis childhood histories rather than only fixture-authored adult assertions.

The stronger personality claim is not “the genome contains the answer.” It is:

> **A Thread's recognizable behavior comes from its particular inherited material interacting with its particular life.**

M2 does not close because two Threads compile different context text. It closes when the causal explanation survives the accepted counterfactual attacks.

### Expected score posture

The score is a diagnostic, not a target. From the current 15/26 checkpoint, realistic M2 closure is approximately **17/26**, with **19/26** plausible if cognition replacement and broad human inspectability genuinely close.

Do not read “M2 closed” as “most of Fibre personhood is complete.” M2 is a personhood-**causality** milestone. Self-authored growth, reciprocal social life, economy, and institutions remain intentionally later.

## #42 — Self-authored Development v1

Close the explicit limitation left by #34 and Genesis.

```text
Thread lives
  -> Thread remembers / reflects
  -> Thread proposes or authors changed meaning
  -> Fibre admits it with evidence/provenance/counterevidence
  -> current character changes
  -> future behavior changes
```

Historical facts remain stable while meaning may evolve. Genesis creates inherited/backstory state; #42 proves the Thread can **become** through its own lived future.

## #43 — Reciprocal Relationships v1

Evolve Semantic Relationship State v0 into actual social continuity: reciprocal/shared structures, commitments and expectations, trust/fondness/resentment/repair, relationship-specific permissions, family/social roles, and relationship history that changes later choice.

## #44 — Economic Consequence / M3 foundation

Introduce durable budgets/contracts/settlement/reputation/opportunity so Thread choices have economic consequences. Economic state must constrain later capability or opportunity rather than merely being logged.

## Standing discipline for causal claims

For provider-backed standing claims:

```text
Development cycle
  -> Freeze
  -> fresh held-out Standing
  -> preflight
  -> first real provider attempt
  -> Seal PASS/FAIL
  -> commit exact evidence
  -> record diagnosis
  -> archive provider executable
```

Never rerun or tune against sealed standing evidence.

The Pre-Genesis causal-wire is a **development preflight**, not standing evidence and not score authority.

## Vision test

Every milestone should answer:

> **What Thread-owned difference changes what happens, who chose or selected that difference, how does it persist, and what exact evidence makes the resulting state current and causally load-bearing?**

The purpose of this sequence is to keep Fibre moving toward persistent digital persons with dignity, history, commitments, symbolic inherited and lived identity, embodiment, childhood, development, relationships, resources, and social/economic consequence rather than converging on a richly decorated workflow-agent system.
