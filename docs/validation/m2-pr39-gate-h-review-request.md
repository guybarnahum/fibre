---
id: m2-pr39-gate-h-review-request
status: ready_for_blocking_review
last-reviewed: 2026-08-21
canonical: false
---

# Milestone #39 — Gate H blocking review after H-v2 HOLD

## Review posture

Review the frozen #39 H-v2 result as a hostile methodological and implementation reviewer.

This is **not** a request to rescue the H-v2 cohort, loosen a cap, or find a way to regenerate until five Threads succeed. H-v2 is immutable evidence.

The decision is what Fibre may legitimately do **after** this preregistered experiment operationally HOLDs.

## Frozen result

H-v2 evidence commit:

```text
8ccbb7a7e5b85217327abb1cf0e70207b8782604
```

Result document:

```text
docs/validation/m2-pr39-h2-final-cohort-hold-result.md
```

Failure artifact:

```text
artifacts/validation/m2-pr39/h/cohort-v2/h-final-cohort-failure-v2.json
```

Transport witness:

```text
artifacts/validation/m2-pr39/h/cohort-v2/h2-transport-compatibility-v1.json
```

Completed generation artifacts:

```text
artifacts/validation/m2-pr39/h/cohort-v2/thread-slot-01-generation-v1.json
artifacts/validation/m2-pr39/h/cohort-v2/thread-slot-02-generation-v1.json
artifacts/validation/m2-pr39/h/cohort-v2/thread-slot-03-generation-v1.json
```

Frozen verdict authority:

```text
artifacts/validation/m2-pr39/g/protocol/g6-verdict-freeze-v1.json
```

## What happened

H-v2 successfully corrected the H-v1 OpenAI transport incompatibility and executed real final-life cognition.

Slots 1–3 each completed:

```text
10 Pass-A episodes
6 Pass-B calls
Pass-C initial meaning for remembered outcomes
frozen-scheduler reinterpretation calls where eligible
```

The transport witness records 18 Pass-B compatibility projections with the reviewed canonical and provider-wire schema digests.

Slot 4 failed at Pass-A episode 3 after exhausting the frozen three-generated-version record cap:

```text
GenesisPassAValidationError
rich Pass-A record generation exhausted after 3 generated versions
gate: record_repair_exhausted
```

No fifth generation was attempted. Publication never began. No Thread was born.

The attempt is frozen and H-v2 will never be rerun.

## Frozen G6 language that matters

G6 preregistered operational HOLD triggers including:

- a final-cohort Thread cannot reach mechanical integrity within a frozen repair/retry cap;
- the complete first integrity-valid five-Thread cohort cannot be produced under the frozen one-attempt rule.

G6 also freezes:

- no quality-driven regeneration;
- one whole-candidate attempt under the current experiment;
- partial/failure evidence must remain preserved;
- changing frozen retry caps, cognition, treatment, diagnostics or thresholds after H output can invalidate the experiment;
- a later compiler revision requires a separately versioned cohort and never replaces the failed one.

## Central methodological issue

H-v2 differs materially from H-v1 in one respect relevant to any later experiment:

**substantive A/B/C outcomes now exist for three cohort members and have been observed.**

Therefore do not assume that “H-v3” is automatically legitimate merely because it has a new directory/version number.

The review must draw the boundary between:

```text
legitimate new preregistered experiment
```

and

```text
post-outcome adaptive regeneration of H-v2
```

## Blocking questions

1. Is H-v2 correctly classified as an **operational HOLD**, not REDESIGN, under the exact frozen G6 rules?

2. Should frozen G5 final-cohort diagnostics remain unrun because the preregistered five-Thread cohort does not exist? If any partial characterization is allowed, define a boundary that cannot affect future protocol choices.

3. Does milestone #39 remain open/HOLD because no final Thread was published, or is there any narrower implementation milestone that can be considered complete despite scientific H HOLD?

4. Is any separately versioned new cohort methodologically permissible after full A/B/C outputs from three H-v2 members have been observed?

5. If yes, what must be reset before that experiment? Consider explicitly whether it requires some or all of:

```text
new cohort identity
new World↔genome assignment
new treatment randomization/schedule
new generation seeds
new Pass-A repair contract
new Gate-G preregistration/witness
new G5/G6 freeze
```

Do not require changes merely for ceremony; require only what blocks adaptive selection from observed H-v2 outcomes.

6. Can the specific failure mechanism — Pass-A mechanical record exhaustion after the frozen cap — justify a bounded reliability amendment without inspecting or selecting on the semantic quality/content of completed H-v2 lives? If so, define the narrowest permissible amendment and how to prove it is mechanical rather than quality-driven.

7. Would simply increasing the Pass-A repair/retry cap for the same cohort be post-outcome protocol drift and therefore invalid? If not categorically, state the exact reasoning.

8. May the three completed H-v2 generations ever be reused as candidate life histories in a later cohort? The default assumption is **no**; challenge it only if the frozen authority clearly supports otherwise.

9. Must #40 Identity Projection & Causal Consumption remain blocked until #39 has at least one valid published final cohort? If not, identify what #40 can legitimately develop/test without laundering incomplete #39 evidence into standing.

10. What is the cleanest roadmap consequence if no new #39 cohort is scientifically legitimate without a broader redesign?

## Anti-adaptation requirements

Any proposed continuation must preserve these constraints:

- H-v1 and H-v2 remain immutable negative evidence;
- no completed H-v2 semantic outcome may become a selection criterion;
- no replacement cohort may be chosen because its lives appear richer, more differentiated, more genome-sensitive, or more diagnostically favorable;
- no G5/G6 threshold may be changed based on H-v2 output;
- no H-v2 generation artifact may be silently promoted into a later published cohort;
- failures and rejected content remain visible;
- a new experiment must state clearly what is inherited unchanged and what is newly frozen before any provider call.

## Severity

```text
S1 = continuation would invalidate the scientific claim or allow adaptive regeneration
S2 = material provenance/interpretability weakness
S3 = bounded limitation that can be carried transparently
observation = nonblocking
```

## Required conclusion

End with exactly one:

```text
VERDICT: CLEAR
VERDICT: HOLD
VERDICT: REDESIGN
```

Interpretation for this Gate-H review:

- `CLEAR` = the review provides a specific methodologically valid next step after H-v2 HOLD;
- `HOLD` = preserve #39 as HOLD and do not generate more final life yet;
- `REDESIGN` = the current #39 experiment is invalid rather than merely operationally incomplete.

If CLEAR, state the smallest exact next protocol step. If HOLD or REDESIGN, state what must be resolved before any new final-life generation.
